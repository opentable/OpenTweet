import Foundation

/// This extension contains all the localized strings being declared in the code
extension String {
    static var timeline: String { "Timeline".localized }
    static var thread: String { "Thread".localized }
}
